const homeButton = document.querySelector('.home-button');

const progressBars = document.querySelectorAll('.profile__bar-front');
const barsSettings = ['100%', '70%', '90%'];

export { homeButton, progressBars, barsSettings };
